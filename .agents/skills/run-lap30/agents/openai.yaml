interface:
  display_name: "Lap30"
  short_description: "製品Taskは原則1周、純粋証跡更新は軽量に処理"
  default_prompt: "Use $run-lap30 to classify work, deliver each product task in one counted lap by default, and keep pure evidence maintenance outside counted laps."
